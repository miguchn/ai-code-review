# 安全政策

## 支持的版本

| 版本 | 支持状态 |
|------|----------|
| main 分支（V0.1 MVP 内部试点） | ✅ 接受漏洞报告 |
| 历史提交 / 未发布版本 | ❌ 不单独维护安全补丁 |

本项目当前处于内部试点阶段，安全修复随 `main` 分支滚动发布，不单独维护 LTS 或回迁补丁。

## 报告漏洞

**请勿通过公开 issue、PR 或讨论区报告安全漏洞。**

请发送邮件至：**miguchn@gmail.com**，邮件标题以 `[ACR Security]` 开头，内容尽量包含：

1. 漏洞描述与影响范围（涉及哪个模块/接口/页面）；
2. 复现步骤或最小 PoC；
3. 相关代码位置（文件/行号，如已知）；
4. 你的联系方式（如需后续沟通与致谢）。

## 响应承诺

- **48 小时内**确认收到并给出初步判断；
- 确认后进入修复流程，修复完成前不公开细节；如需协调披露时间，会与你协商；
- 修复合入 `main` 后，在 [CHANGELOG.md](CHANGELOG.md) 中以常规条目记录（默认不披露漏洞细节，除非与你协商一致）；
- 对善意、负责任的报告者公开致谢（除非你要求匿名）。

## 范围参考

以下类型属于重点关注范围（非穷举）：

- 凭据相关：Git PAT / Webhook Secret / 通知渠道 / 模型 API Key 的加密、回显、日志泄露；
- 认证与授权：登录、JWT、`@PreAuthorize` 权限串、`@DataScope` 数据范围绕过；
- Webhook 验签与重放：伪造事件、去重绕过；
- 注入与 XSS：SQL 注入、存储/反射型 XSS、路径穿越；
- 主密钥 `ACR_CREDENTIAL_MASTER_KEY` 相关的设计缺陷。

以下不在范围内：

- 依赖组件的已知 CVE（除非可证明在本项目中可被利用，欢迎一并报告）；
- 需要物理接触或本机已沦陷前提的问题；
- 纯社会工程学攻击。

感谢你帮助本项目保持安全。
